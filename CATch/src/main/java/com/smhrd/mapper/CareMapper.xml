<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.smhrd.mapper.CareMapper">
    <!-- 급식소 주소,위도,경도 가져오는 sql문 -->
    <select id="latlng" parameterType="HashMap" resultType="HashMap" >
	 <![CDATA[	
        select  care_latitude AS LAT,care_longitude AS LNG
		 from t_carezone
		 WHERE
   			 care_latitude > (#{LAT}-0.002) AND care_latitude < (#{LAT}+0.002) AND care_longitude > (#{LNG}-0.002) AND care_longitude < (#{LNG}+0.002)
   	    ]]>
	</select>
	

</mapper>